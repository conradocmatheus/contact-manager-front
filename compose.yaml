services:
  front:
    image: node:22-alpine
    container_name: contact-manager-front
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "4200:4200"
    command: sh -c "npm install && npm run start -- --host 0.0.0.0 --poll=2000"
